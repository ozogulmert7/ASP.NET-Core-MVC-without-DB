@{
    ViewData["Title"]="Blog Single";
    ViewData["Subtitle"]="The Best Yoga Time is Early Morning with the empty stomach";
}
@{await Html.RenderPartialAsync("_BannerAnother");}
@{await Html.RenderPartialAsync("_BlogSingle");}
@{await Html.RenderPartialAsync("_BlogPageAnother");}

